[project]
name = "siemsalabim"
version = "0.1.0"
description = "Simple SIEM"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[workspace]
members = ["apps/*"]

[tool.uv.workspace]
members = [
    "apps/engine",
    "apps/exporter",
    "apps/dashboard",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.13",
]
